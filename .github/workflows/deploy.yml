name: Github Actions 실행하기

on:
    push:
        branches:
            - main

jobs:
    My-Deploy-Job:
        runs-on: Ubuntu-latest

        steps:
            - name: Hello World 찍기
              run: echo "Hello World"